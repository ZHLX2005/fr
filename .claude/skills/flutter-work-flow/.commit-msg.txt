docs(skills): consolidate Flutter-TimePage to single long ref (no over-split)

Per user feedback "ref 太少了 / 不要字数分割 / 全部都是 timePage 主题里面呀" —
the previous split into 4 specialized refs (新增/改面板/统计与心流/Lab过滤) was
over-fragmentation. key_board_3 main idea: ref is "特化场景指导, not for the
sake of splitting". The 4 aspects are all under the timePage topic; putting
them in separate files duplicated abstraction definitions (DemoPage.timePage,
kTimePageMeta, TimePageMeta appear in all 4) and required inter-file references.

Now collapsed to one 435-line ref with internal chapters. SKILL.md index row
returns to single entry. Memory pointer updated to reflect new path.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
