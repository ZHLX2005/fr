docs(skills): split Flutter-TimePage ref into 1 main + 4 specialized refs

Per the user feedback "ref 太少了" — extract 4 specialized refs from the
previously bundled monolith. Each ref now corresponds 1:1 to a distinct
action scope so the right ref loads just-in-time, not the whole ~190-line
bundle.

The four specialized refs:
- 新增timePage工作流 (3-step SOP + do/dont examples)
- 修改Focus面板 (6 modification motivations + boundary guards)
- 统计与心流空间扩展 (4-step SOP + anti-patterns)
- Lab过滤与深链 (4 invariants + self-check checklist)

Main "Flutter-TimePage-Focus时间模块设计" now holds only architecture,
key abstractions, 3 data flows, and the gamecenter dual-table — ~92 lines.
SKILL.md total length stays healthy at 85 lines.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
