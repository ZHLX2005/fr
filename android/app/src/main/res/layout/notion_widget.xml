<?xml version="1.0" encoding="utf-8"?>
<!--
  Notion 图床桌面小组件 — 1x1 极简版。

  核心策略：
    - 整个 widget 只有「拍照」按钮一个元素，撑满 match_parent
    - 不再保留任何 header / 副标题 / 装饰 — 1x1 区域本就只够一个交互按钮
    - 2x2 拉大时按钮仍居中显示（FrameLayout gravity center 自适应）
    - 文字「拍照」+ 紫色文字色 + 圆角描边背景，靠风格而非体积传达品牌

  交互：
    - 点击按钮（整个 widget 唯一可点元素）→ 触发 autocapture=1 的 deep link
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <TextView
        android:id="@+id/widget_capture_btn"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="拍照"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#673AB7"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="拍照并上传到 Notion" />

</FrameLayout>