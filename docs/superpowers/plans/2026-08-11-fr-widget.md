# fr 任务 #5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 fr 项目 kvcli todo `fr` topic 任务 #5——clock 桌面小组件区分「等待开始」/「已暂停」/「进行中」/「已超时」四态，并新增单一 toggle 按钮（点击进 app toggle 最新的 clock）。

**Architecture:** Dart 侧加 `isPausedAtStart` 字段（`!isRunning && remainingSeconds == durationSeconds`）+ `LabClockProvider.toggleLatestClock()`（无→无操作 / 运行中→pause / 否则→start）；新增 `fr://clock/widget-toggle` 专用路由（仿 RecorderHandler）→ ClockDemo 进入后消费 pending toggle。Native 侧 layout 加 `widget_toggle_btn`（ImageButton 48dp 底中），`ClockWidgetProvider.kt` 重构三态→四态 + 给按钮设 PendingIntent 走 `fr://clock/widget-toggle` + 按状态切换播放/暂停图标。

**Tech Stack:** Flutter (Dart), provider, home_widget plugin（qualifiedAndroidName 路径），Android RemoteViews + AppWidgetProvider + PendingIntent，fr:// 路由体系（仿 RecorderHandler）。

## Global Constraints

- 每簇完成后 `flutter analyze` 必须 **0 新增 error/warning**（基线：当前 master 干净）。
- 每簇 analyze 干净后立即 `git add/commit/push`（[[feedback_autocommit_on_fix]]，不要问用户）。
- **test/ 被 .gitignore**（[[test_gitignore_force_add]]）：新增测试文件一律 `git add -f test/...`。
- 完成后 `kvcli todo done 5 --result "..."` 回填。
- 沿用 RecorderHandler 模式：`fr://clock/widget-toggle` → `ClockWidgetToggleHandler` → `_ClockWidgetTogglePage`（包装 ClockDemo，调 `markClockWidgetTogglePending()`）→ `_ClockDemoState.didChangeDependencies` 消费 flag 调 `LabClockProvider.toggleLatestClock()`。
- 不动 recorder/notion widget；不新增 drawable；不修改 `HomeWidget.updateWidget(qualifiedAndroidName: ...)` 调用方式。
- 颜色用 native 已有常量：`running 0xFF4CAF50` / `paused 0xFF757575` / 新增 `pausedAtStart 0xFF9E9E9E`。
- Flutter 命令在仓库根 `D:\code\a_dart\prj\fr` 执行。

---

# Part — clock widget UI + 按钮层（#5）

## Task 1: ClockWidgetData 新增 isPausedAtStart + 单测

**Files:**
- Modify: `lib/native/home_widget/clock_widget_data.dart`（构造、`empty`、`fromClock`、`toMap`）
- Test: `test/native/home_widget/clock_widget_data_test.dart`（新建）

**Interfaces:**
- Produces: `ClockWidgetData({... required this.isPausedAtStart})`、`fromClock` 自动推导、`toMap` 序列化为 `'isPausedAtStart': isPausedAtStart ? 1 : 0`、`empty.isPausedAtStart == false`

- [ ] **Step 1: 写失败测试**

```dart
// test/native/home_widget/clock_widget_data_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:xiaodouzi_fr/native/home_widget/clock_widget_data.dart';

void main() {
  group('ClockWidgetData.isPausedAtStart', () {
    test('空数据 isPausedAtStart=false', () {
      expect(ClockWidgetData.empty.isPausedAtStart, isFalse);
    });
    test('新建后从未启动（!isRunning && remaining == duration）→ true', () {
      final d = ClockWidgetData.fromClock(
        title: 't', remainingSeconds: 60, durationSeconds: 60,
        isRunning: false, color: '#D4644B',
      );
      expect(d.isPausedAtStart, isTrue);
    });
    test('中途暂停（!isRunning && remaining < duration）→ false', () {
      final d = ClockWidgetData.fromClock(
        title: 't', remainingSeconds: 30, durationSeconds: 60,
        isRunning: false, color: '#D4644B',
      );
      expect(d.isPausedAtStart, isFalse);
    });
    test('运行中 → false', () {
      final d = ClockWidgetData.fromClock(
        title: 't', remainingSeconds: 30, durationSeconds: 60,
        isRunning: true, color: '#D4644B',
      );
      expect(d.isPausedAtStart, isFalse);
    });
    test('toMap 包含 isPausedAtStart 键值', () {
      final d = ClockWidgetData.empty;
      expect(d.toMap()['isPausedAtStart'], 0);
    });
  });
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `flutter test test/native/home_widget/clock_widget_data_test.dart`
Expected: FAIL — `isPausedAtStart` 未定义。

- [ ] **Step 3: 实现 isPausedAtStart**

`lib/native/home_widget/clock_widget_data.dart` 修改：

构造参数列表（`const ClockWidgetData({...})` 第 32-42 行区域），追加：
```dart
  /// 是否"等待开始"语义：未运行 + remainingSeconds == durationSeconds。
  /// native 用此区分"新建未启动" vs "中途暂停"，文案/颜色不同。
  final bool isPausedAtStart;
```

并加进构造参数：
```dart
    required this.isPausedAtStart,
  });
```

`fromClock` 工厂（第 45-73 行）自动推导：在 `final isOvertime = remainingSeconds < 0;` 之前/之后加：
```dart
    final isPausedAtStart = !isRunning && remainingSeconds == durationSeconds;
```
并在返回的 `ClockWidgetData(...)` 第 61-72 行区域加 `isPausedAtStart: isPausedAtStart,`。

`empty` 常量（第 76-86 行）追加：
```dart
    isPausedAtStart: false,
  );
```

`toMap()`（第 89-101 行）追加：
```dart
      'isPausedAtStart': isPausedAtStart ? 1 : 0,
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `flutter test test/native/home_widget/clock_widget_data_test.dart`
Expected: PASS（5 用例）。

## Task 2: ClockWidgetService 写入 isPausedAtStart

**Files:**
- Modify: `lib/native/home_widget/clock_widget_service.dart`（新增 key + Future.wait 多一行）

**Interfaces:**
- Consumes: `ClockWidgetData.isPausedAtStart`（Task 1）

- [ ] **Step 1: 新增 SharedPreferences key**

`clock_widget_service.dart` 第 24 行 `_keyStartRemainingSeconds` 之后加：
```dart
  static const String _keyIsPausedAtStart = 'clock_is_paused_at_start';
```

- [ ] **Step 2: Future.wait 写入新字段**

`updateClockWidget` 内 `Future.wait([...])` 第 36-58 行区域，在最后（`_keyStartRemainingSeconds` 之后）追加：
```dart
        HomeWidget.saveWidgetData(
          _keyIsPausedAtStart,
          data.isPausedAtStart ? '1' : '0',
        ),
```

- [ ] **Step 3: analyze**

Run: `flutter analyze lib/native/home_widget`
Expected: 0 issues。

## Task 3: LabClockProvider.toggleLatestClock + 单测

**Files:**
- Modify: `lib/lab/demos/clock/providers/lab_clock_provider.dart`（在 `pauseCountdown` 之后新增公共方法）
- Test: `test/lab/clock_toggle_latest_test.dart`（新建）

**Interfaces:**
- Produces: `LabClockProvider.toggleLatestClock() -> Future<void>`（无 clock→无操作；运行中→`pauseCountdown`；否则→`startCountdown`）

- [ ] **Step 1: 写失败测试**

```dart
// test/lab/clock_toggle_latest_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:xiaodouzi_fr/lab/demos/clock/providers/lab_clock_provider.dart';

void main() {
  test('toggleLatestClock 在无 clock 状态下安全无操作', () async {
    final p = LabClockProvider();
    // 不预置 clocks：直接调 toggleLatestClock，应无 throw 且 _clocks 仍空
    await p.toggleLatestClock();
    expect(p.clocks, isEmpty);
  });
}
```

> 注：`LabClockProvider()` 构造会启动 `_startTimer`（每秒 Timer）、`MetronomeService.ensureReady`、`SampleLoader.restoreAtStartup`、`loadClocks`。在 flutter_test 中这些调用不会 throw（Timer 在 test zone 内静止；FFI 兜底；SharedPreferences.setMockInitialValues() 默认空 prefs）。若 `MetronomeService.instance.ensureReady()` 或 `SampleLoader.restoreAtStartup()` 抛错/挂起，用 `SharedPreferences.setMockInitialValues({});` + 接受 Timer 静默；若仍有挂起，将测试仅覆盖「无操作分支」（不创建 clock）。

- [ ] **Step 2: 运行测试，确认失败**

Run: `flutter test test/lab/clock_toggle_latest_test.dart`
Expected: FAIL — `toggleLatestClock` 未定义。

- [ ] **Step 3: 实现 toggleLatestClock**

`lab_clock_provider.dart` 在 `pauseCountdown`（第 314-329 行附近，`Future<void> pauseCountdown(String id) async {` 块结束之后、`resetCountdown` 之前）新增：

```dart
  /// Toggle 最新的 clock（_clocks.first）：
  /// - 无 clock → 无操作
  /// - 运行中 → pauseCountdown
  /// - 等待开始或已暂停（!isRunning） → startCountdown
  /// 桌面小组件 toggle 按钮专用入口（fr #5）。
  Future<void> toggleLatestClock() async {
    if (_clocks.isEmpty) return;
    final clock = _clocks.first;
    if (clock.isRunning) {
      await pauseCountdown(clock.id);
    } else {
      await startCountdown(clock.id);
    }
  }
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `flutter test test/lab/clock_toggle_latest_test.dart`
Expected: PASS（1 用例）。

## Task 4: clock widget toggle 路由 handler

**Files:**
- Create: `lib/core/schema/handlers/clock_widget_toggle_handler.dart`

**Interfaces:**
- Produces: `ClockWidgetToggleHandler.build` 返回 `_ClockWidgetTogglePage`，调 `markClockWidgetTogglePending()` 标记 pending toggle（仿 RecorderHandler 的 `markRecorderAutoStart()`）。

- [ ] **Step 1: 实现 handler（仿 RecorderHandler）**

```dart
// lib/core/schema/handlers/clock_widget_toggle_handler.dart
import 'package:flutter/material.dart';

import '../fr_route_handler.dart';
import '../../../lab/demos/clock_demo.dart'
    show ClockDemoPage, markClockWidgetTogglePending;

/// fr://clock/widget-toggle → 时钟 demo 并自动 toggle 最新的 clock。
///
/// 桌面 widget 点击 toggle 按钮后：PendingIntent → MainActivity →
/// fr://clock/widget-toggle 路由 → 本 handler 返回包装页 →
/// `markClockWidgetTogglePending()` 标记 pending → ClockDemoPage.didChangeDependencies
/// 消费 flag → 调 LabClockProvider.toggleLatestClock()。
///
/// 与 RecorderHandler 同样的模式：专用 handler 在 router 阶段 prefix
/// 命中后，按 query/authority 验证 + 触发额外动作。
class ClockWidgetToggleHandler extends FrRouteHandler {
  const ClockWidgetToggleHandler();

  @override
  Widget build(BuildContext context, FrRouteMatch match) {
    assert(
      match.authority == 'clock/widget-toggle',
      'ClockWidgetToggleHandler 期望 authority=clock/widget-toggle,实际: ${match.authority}',
    );
    // 标记 pending —— ClockDemoPage mount 后消费。
    // 不要在此处直接调 toggleLatestClock(),因为 ClockDemoPage 还在构造中,
    // ChangeNotifierProvider 还没 attach。
    markClockWidgetTogglePending();
    return const ClockDemoPage();
  }
}
```

> 注：上面引用 `ClockDemoPage` 和 `markClockWidgetTogglePending`——这两个符号在 Task 5 才新增；本 Task 只写 handler 文件，编译会在 Task 5 之后通过。

## Task 5: ClockDemo 加 auto-toggle pending 消费

**Files:**
- Modify: `lib/lab/demos/clock_demo.dart`（导出 `markClockWidgetTogglePending` + `clockWidgetTogglePending` + `ClockDemoPage`）
- Test: 无（pending flag 是静态模块级变量，测试价值低）

**Interfaces:**
- Produces: `void markClockWidgetTogglePending()`、`bool clockWidgetTogglePending`（仿 RecorderHandler 的 `recorderPageKey`/`markRecorderAutoStart`/`recorderAutoStartPending`）

> 当前 `clock_demo.dart` 没有 StatefulWidget demo page，ClockDemo.buildPage 直接返回 `_ClockShell`。需要把 `_ClockShell` 包成 StatefulWidget（仿 RecorderDemoPage），或者新增一个轻量的 `_ClockDemoStateful` 仅持有 auto-toggle 消费逻辑。前者改动小、贴近 Recorder 模式。

- [ ] **Step 1: 把 `_ClockShell` 包成 StatefulWidget（仿 RecorderDemoPage）**

`lib/lab/demos/clock_demo.dart` 顶部 import 区追加：
```dart
import 'package:flutter/foundation.dart' show visibleForTesting;
import 'providers/lab_clock_provider.dart' as p;
```

在 `registerClockDemo()` 函数前（约第 220 行之前）新增 pending flag 模块级变量 + 标记 + 消费函数：

```dart
/// 桌面 widget toggle 入口 — 若当前页不是 ClockDemoPage(冷启动/正在别的页),
/// 则标记一次 pending toggle,等 ClockDemoPage mount 时再触发。
final ValueNotifier<bool> _pendingWidgetToggle = ValueNotifier(false);

/// 标志:widget 点击后是否需要自动 toggle 最新的 clock。
/// `true` 时 ClockDemoPage 第一次 mount → 自动调 toggleLatestClock。
bool get clockWidgetTogglePending => _pendingWidgetToggle.value;

/// 由桌面 widget click → MainActivity handleIntent → fr://clock/widget-toggle
/// 路由 → ClockWidgetToggleHandler.build 时调用。
void markClockWidgetTogglePending() {
  _pendingWidgetToggle.value = true;
}

/// ClockDemoPage mount 后由 state 调一次,消费 pending flag
/// 并实际 toggle 最新 clock。
@visibleForTesting
Future<void> consumeClockWidgetToggle(ClockProviderRef ref) async {
  if (!_pendingWidgetToggle.value) return;
  _pendingWidgetToggle.value = false;
  await ref.toggleLatestClock();
}
```

> `ClockProviderRef` 需要在 LabClockProvider 内新增 typedef 或直接用 `LabClockProvider` 类型。为最小改动，使用 `LabClockProvider`：

```dart
@visibleForTesting
Future<void> consumeClockWidgetToggle(LabClockProvider provider) async {
  if (!_pendingWidgetToggle.value) return;
  _pendingWidgetToggle.value = false;
  await provider.toggleLatestClock();
}
```

在 `class ClockDemo` 后新建 StatefulWidget（仿 RecorderDemoPage）：

```dart
/// ClockDemo 页面包装 —— 由 ClockDemo.buildPage 返回。
/// 用于消费桌面 widget toggle pending flag(类似 RecorderDemoPage 的 autostart)。
class ClockDemoPage extends StatefulWidget {
  const ClockDemoPage({super.key});

  @override
  State<ClockDemoPage> createState() => _ClockDemoPageState();
}

class _ClockDemoPageState extends State<ClockDemoPage> {
  bool _widgetToggleConsumed = false;

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
    if (!_widgetToggleConsumed && clockWidgetTogglePending) {
      _widgetToggleConsumed = true;
      WidgetsBinding.instance.addPostFrameCallback((_) async {
        final provider = context.read<LabClockProvider>();
        await consumeClockWidgetToggle(provider);
      });
    }
  }

  @override
  Widget build(BuildContext context) {
    // 委托给原有 _ClockShell(它内部已包含 LabClockProvider 树)。
    return const _ClockShell();
  }
}
```

- [ ] **Step 2: 改 `ClockDemo.buildPage` 返回 `ClockDemoPage`**

`clock_demo.dart` 第 33-37 行：
```dart
    return ChangeNotifierProvider(
      create: (_) => LabTrackProvider()..loadTracks(),
      child: const _ClockShell(),
    );
```
→
```dart
    return ChangeNotifierProvider(
      create: (_) => LabTrackProvider()..loadTracks(),
      child: const ClockDemoPage(),
    );
```

> 注意：`ClockDemoPage.build` 内部 return `const _ClockShell()` —— 需要在 `ClockDemoPage` 的 build 中读 `LabClockProvider`。但全局 `LabClockProvider` 在 `main.dart` 已注册（global），可用 `context.read<LabClockProvider>()` 获取。

- [ ] **Step 3: analyze**

Run: `flutter analyze lib/lab/demos/clock lib/core/schema/handlers/clock_widget_toggle_handler.dart`
Expected: 0 issues。

## Task 6: bootstrap_routes.dart 注册新路由

**Files:**
- Modify: `lib/core/schema/bootstrap_routes.dart`（`registerAll` 列表加一行 + import）

**Interfaces:**
- Consumes: `ClockWidgetToggleHandler`（Task 4）

- [ ] **Step 1: import**

`bootstrap_routes.dart` import 区追加：
```dart
import 'handlers/clock_widget_toggle_handler.dart';
```

- [ ] **Step 2: registerAll 加 FrRoute**

`registerAll` 列表（第 19-27 行）任意位置追加（建议放在 recorder 之后保持 demo 类路由相邻）：
```dart
    FrRoute('clock/widget-toggle', handler: const ClockWidgetToggleHandler()),
```

- [ ] **Step 3: analyze**

Run: `flutter analyze lib/core/schema`
Expected: 0 issues。

## Task 7: Native layout 加 widget_toggle_btn

**Files:**
- Modify: `android/app/src/main/res/layout/clock_widget.xml`

**Interfaces:**
- Produces: `ImageButton` `@+id/widget_toggle_btn`（48dp, bottom|center_horizontal, 透明背景）

- [ ] **Step 1: 在 widget_container 内 LinearLayout 后追加 ImageButton**

`clock_widget.xml` 在 `</LinearLayout>`（第 66 行）后、`</FrameLayout>`（第 68 行）前，插入：

```xml
    <!-- 新增：toggle 按钮层（单一 ImageButton，点击进 app toggle 最新 clock）。
         图标/着色由 Kotlin updateAppWidget 按状态动态切换（运行中→暂停图标，
         否则→播放图标）。RemoteViews 支持 ImageButton + setOnClickPendingIntent。 -->
    <ImageButton
        android:id="@+id/widget_toggle_btn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="12dp"
        android:scaleType="fitCenter"
        android:background="@android:color/transparent"
        android:src="@drawable/widget_ic_running"
        android:contentDescription="开始/暂停时钟" />

```

## Task 8: Native ClockWidgetProvider 加 toggle PendingIntent + 四态分支

**Files:**
- Modify: `android/app/src/main/kotlin/io/github/xiaodouzi/fr/native/widget/ClockWidgetProvider.kt`

**Interfaces:**
- Consumes: SharedPreferences 新 key `clock_is_paused_at_start`（Task 2 写入）

- [ ] **Step 1: 重构三态 → 四态分支**

`ClockWidgetProvider.kt` 第 76-80 行：
```kotlin
            val style = when {
                isOvertime -> StatusStyle("已超时", R.drawable.widget_ic_overtime, R.drawable.status_pill_overtime, 0xFFE64A19.toInt())
                isRunning -> StatusStyle("进行中", R.drawable.widget_ic_running, R.drawable.status_pill_running,  0xFF4CAF50.toInt())
                else       -> StatusStyle("已暂停", R.drawable.widget_ic_paused, R.drawable.status_pill_paused,   0xFF757575.toInt())
            }
```
→
```kotlin
            val isPausedAtStart = widgetData.getString("clock_is_paused_at_start", "0") == "1"
            val style = when {
                isOvertime      -> StatusStyle("已超时", R.drawable.widget_ic_overtime, R.drawable.status_pill_overtime, 0xFFE64A19.toInt())
                isRunning       -> StatusStyle("进行中", R.drawable.widget_ic_running, R.drawable.status_pill_running,  0xFF4CAF50.toInt())
                isPausedAtStart -> StatusStyle("等待开始", R.drawable.widget_ic_paused, R.drawable.status_pill_paused,   0xFF9E9E9E.toInt())
                else            -> StatusStyle("已暂停", R.drawable.widget_ic_paused, R.drawable.status_pill_paused,   0xFF757575.toInt())
            }
```

- [ ] **Step 2: 在 `apply {` 末尾追加 toggle 按钮的 PendingIntent + 图标**

`ClockWidgetProvider.kt` 第 82-120 行 `apply {` 块内，在最后一行 `setOnClickPendingIntent(R.id.widget_icon, refreshPi)`（第 119 行）之后、`}` 之前追加：

```kotlin
                // 新增：toggle 按钮 PendingIntent → fr://clock/widget-toggle
                // requestCode 用 appWidgetId + 1000 避开主体 (0) 与刷新图标 (appWidgetId)
                val toggleIntent = Intent(context, MainActivity::class.java).apply {
                    action = Intent.ACTION_VIEW
                    data = android.net.Uri.parse("fr://clock/widget-toggle")
                    flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TOP
                }
                val togglePi = PendingIntent.getActivity(
                    context,
                    appWidgetId + 1000,
                    toggleIntent,
                    PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
                )
                setOnClickPendingIntent(R.id.widget_toggle_btn, togglePi)

                // toggle 按钮图标按状态切换：运行中显示暂停图标，否则显示播放图标
                val toggleIconRes = if (isRunning) R.drawable.widget_ic_paused else R.drawable.widget_ic_running
                setImageViewResource(R.id.widget_toggle_btn, toggleIconRes)
                setColorInt(R.id.widget_toggle_btn, "setColorFilter", style.color, style.color)
```

## Task 9: analyze + 真机验证 + commit + todo done

- [ ] **Step 1: analyze**

Run: `flutter analyze`
Expected: 0 issues（base 既有 warning 不在本次改动文件内即可）。

- [ ] **Step 2: 真机验证（交给用户）**

热重启 / 重新安装 → 添加 clock widget → 验证：
- 新建一个 clock（未启动）：widget 显示「等待开始」+ 浅灰 + 按钮显示播放图标。
- 点按钮 → 进 ClockDemo → clock 自动开始 → widget 立即更新（依赖 `_syncToWidget()` 已覆盖所有状态变化点）→ 显示「进行中」+ 绿色 + 按钮变暂停图标。
- 再点按钮 → 暂停 → 显示「已暂停」+ 灰色 + 按钮恢复播放图标。
- 已存在的老 widget 实例（无 `clock_is_paused_at_start` key）应走 `else` 分支显示「已暂停」，向后兼容。

- [ ] **Step 3: commit + push**

```bash
git add lib/native/home_widget/clock_widget_data.dart lib/native/home_widget/clock_widget_service.dart \
        lib/lab/demos/clock/providers/lab_clock_provider.dart \
        lib/core/schema/handlers/clock_widget_toggle_handler.dart \
        lib/lab/demos/clock_demo.dart \
        lib/core/schema/bootstrap_routes.dart \
        android/app/src/main/res/layout/clock_widget.xml \
        android/app/src/main/kotlin/io/github/xiaodouzi/fr/native/widget/ClockWidgetProvider.kt \
        docs/superpowers/specs/2026-08-11-fr-widget-意图.md \
        docs/superpowers/plans/2026-08-11-fr-widget.md
git add -f test/native/home_widget/clock_widget_data_test.dart test/lab/clock_toggle_latest_test.dart
git commit -m "feat(widget): clock widget 区分等待开始 + toggle 按钮

#5: ClockWidgetData 加 isPausedAtStart 字段(等待开始 vs 已暂停);
    native ClockWidgetProvider 重构三态→四态, layout 加 widget_toggle_btn
    (48dp ImageButton 底中); 点击 → fr://clock/widget-toggle 路由 →
    ClockWidgetToggleHandler → ClockDemo 消费 pending → 调
    LabClockProvider.toggleLatestClock()(运行中→pause / 否则→start)。
单测过(isPausedAtStart 5 + toggleLatestClock 1), analyze 干净。
kvcli fr #5."
git push
```

- [ ] **Step 4: 回填 todo**

```bash
kvcli todo done 5 --result "clock widget UI+toggle 完成: ClockWidgetData 加 isPausedAtStart; native 重构三态→四态, 加 widget_toggle_btn (ImageButton 48dp 底中) + toggle PendingIntent (fr://clock/widget-toggle, appWidgetId+1000); 路由 handler + ClockDemoPage 消费 pending → LabClockProvider.toggleLatestClock(). 6 单测过, analyze 干净。需真机目视四态切换+toggle。commit <hash>"
```

---

## Self-Review

- **Spec 覆盖**：意图文档 3 改动点 → Task 1-8 全覆盖。状态显示（Task 1/2/8）、按钮层（Task 7）、toggle 路由（Task 4/5/6）、`toggleLatestClock`（Task 3）。
- **占位符扫描**：所有代码步骤均含实际内容；无「TBD/类似 Task N/以实际为准」。
- **类型一致**：`ClockWidgetData.isPausedAtStart`（Task 1-2）、`LabClockProvider.toggleLatestClock`（Task 3/5）、`ClockWidgetToggleHandler`/`markClockWidgetTogglePending`/`clockWidgetTogglePending`/`consumeClockWidgetToggle`（Task 4/5）、`FrRoute('clock/widget-toggle', ...)`（Task 6）、`widget_toggle_btn` + `clock_is_paused_at_start`（Task 7-8）跨任务命名一致。
- **测试覆盖**：Task 1（5 用例）+ Task 3（1 用例）= 6 单测，data 类纯函数 + provider 无 clock 分支均不碰 FFI/原生，test/ 文件用 `git add -f` 跟踪。
- **向后兼容**：native 老 widget 实例 prefs 无 `clock_is_paused_at_start` → `getString` 默认 `"0"` → 走 `else` 分支显示「已暂停」，行为不变（Task 8 Step 1）。
- **风险点**：PendingIntent requestCode 用 `appWidgetId + 1000` 避开主体 (0) 与刷新图标 (`appWidgetId`)，多实例互不干扰；toggle 后 widget 立即刷新依赖 `_syncToWidget()` 已覆盖所有状态变化点，不另写 native→Dart 同步。